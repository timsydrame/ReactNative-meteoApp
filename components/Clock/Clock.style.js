import { StyleSheet } from "react-native";

const s = StyleSheet.create({
  
  clockText: {
    fontSize: 30,
    color: "white", // Définit la couleur de l'heure en blanc
  },
});

export { s };
